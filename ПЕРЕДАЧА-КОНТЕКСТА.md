> ⚠️ **ЭТА КОПИЯ УСТАРЕЛА.** Так выглядел файл при первой выгрузке в
> репозиторий. Актуальная версия (v1.5.2 / code 22, раздел 13 про git) —
> [`tg-bot-control/ПЕРЕДАЧА-КОНТЕКСТА.md`](tg-bot-control/ПЕРЕДАЧА-КОНТЕКСТА.md).
> Читай её. Эта копия оставлена для истории.

# ПЕРЕДАЧА КОНТЕКСТА: проект BotControl (читать ПЕРВЫМ ДЕЛОМ)

Этот файл — полная память проекта для продолжения работы в новом диалоге.
Прочитай его целиком до выполнения любой задачи. Workspace сохранён и
является источником истины; если его нет — см. раздел 12 (восстановление).

---

## 1. Что за проект

**BotControl** — Android-приложение (Kotlin + Jetpack Compose, пакет
`com.botcontrol.admin`), которое превращает телефон в хост локальных
Telegram-ботов:,long polling через Telegram Bot API, LLM **строго на
устройстве** (LiteRT-LM / MediaPipe, .litertlm модели, без облака),
мультиботы на разных токенах, всё настраивается кнопками в приложении.

Плюс формат «конфигов кодом»: настройки бота описываются python-подобным
текстом и импортируются/экспортируются приложением (это транспортный
формат, код НЕ исполняется).

Есть также серверная часть (FastAPI + aiogram3) в задумке, но активно
разрабатывался только Android-клиент (local mode).

## 2. Текущее состояние

- **Версия: v1.5.1 (versionCode 21)** — собрана и доставлена.
  APK: `tg-bot-control/BotControl-debug.apk` (94 МБ, debug, пакет
  `com.botcontrol.admin.debug`).
- Что внутри (вкратце): мультиботы, правила-сценарии (команда/фраза/кнопка
  → текст/набор/скрипт JS/ИИ), inline-кнопки (включая URL-кнопки),
  клавиатура чата, напоминания с днями недели + публикация в канал
  (флаг «в канал», ротация текстов через `" | "`), режим «Объявления»
  (визард: описание → контакт → фото до 3 → предпросмотр → публикация
  в канал с кнопкой «Написать автору» tg://user?id=…, статистика,
  переопубликовать/удалить из канала), импорт/экспорт конфигов,
  файловый сервер NanoHTTPD (:8090, media/scripts/docs per-bot),
  журнал бота + общий лог (копирование, экспорт .txt), проблемы бота
  в карточке до устранения, вырезание `<think>` из ответов LLM,
  сброс настроек бота до заводских.
- **versionCode следующей доставки: 22 (v1.5.2 или выше).** Всегда бампать
  перед сборкой для доставки.

## 3. Карта файлов (всё под `/home/user/tg-bot-control/`)

| Путь | Роль |
|---|---|
| `BotControl-debug.apk` | последний доставленный APK (v1.5.1/code 21) |
| `android/` | исходники Gradle-проекта (AGP 8.12.0, Gradle 8.13, Kotlin 2.3.21, KSP 2.3.12, Room 2.7.1, compileSdk 35) |
| `android/app/build.gradle.kts` | versionCode/versionName + зависимости (в т.ч. `org.nanohttpd:nanohttpd:2.3.1`) |
| `scripts/build_in_sandbox.sh` | сборка: сам поднимает JDK17/SDK/AAR/swap; лог `/tmp/build.log`, конец по `SCRIPT_EXIT=0` |
| `bot-config-ready.py` | готовый конфиг «Агент Смит / перекур» (100 шуток, расписание будни+пятница) |
| `daromvpl-channel-ready.py` | готовый конфиг канала @daromvpl: приветствие, /help, /rules, дайджесты 10:20 и 20:40 (флаг `»канал`, ротация по дням) |
| `daromvpl-bot-ready.py`, `channel-bot-ready.py` | ранние конфиги ботов объявлений |
| `Инструкция-код-бота.md` | как писать/править конфиги + чек-лист проблем |
| `Промт-создание-ботов.md` | промт для генерации новых конфигов (с реестром зарезервированных имён) |
| `Фичи-даромвпл.md` | roadmap фич (P0/P1/P2) по боту объявлений |
| `ПРОМТ-НОВОЕ-ОКНО.txt` | готовый текст первого сообщения в новом диалоге |
| `botcontrol-handoff.zip` | архив исходников+доков для переноса в чистый workspace |
| `/home/user/uploads/` | ораклы: `oracle code.txt` (перкур-бот python/Termux — функциональный эталон), `oracle llm.txt`, `правки.txt` |

### Ключевые исходники (`android/app/src/main/java/com/botcontrol/admin/`)

| Файл | Роль |
|---|---|
| `service/LocalBotService.kt` | опрос Telegram per-bot, обработка сообщений/callback, планировщик напоминаний (в т.ч. `toChannel`+ротация), проблемы бота (`problems: Map<String,String>`), интеграция ListingEngine |
| `service/BotBrain.kt` | единый «мозг»: правила → скрипт/набор → ИИ (контекст) → уточняющий/запасной; кулдаун только на «незнакомых» |
| `service/ListingEngine.kt` | визард объявлений (описание→контакт→фото→предпросмотр→публикация), «Мои объявления», кнопки `lst_*` |
| `service/FileServer.kt` | NanoHTTPD :8090 — страница загрузки/скачивания файлов ботов по Wi-Fi |
| `data/telegram/TelegramApi.kt` | getUpdates/sendMessage/sendTo(канал)/sendPhoto/sendPhotoTo/getFile/downloadFile/deleteMessage/editMessageText/answerCallbackQuery/setMyCommands/deleteWebhook |
| `data/LocalBotStore.kt` | DataStore per-bot: клавиатура, промпт, ИИ-параметры, расписание, меню, канал, объявления (`listings`), `resetBot()` |
| `data/BotExtras.kt` | `InlineBtn`(+`url`), `ScheduleEvent`(+`toChannel`), `Listing`, `BotJson`, `withIds()` (детерминированные callback_data) |
| `data/ScriptImporter.kt` | грамматика импорта (см. раздел 5) |
| `data/SettingsExporter.kt` | экспорт настроек в python-конфиг |
| `data/BotFiles.kt` | директории `bot_<id>/media|scripts|docs` |
| `data/PerkurPresets.kt` | пресет перкур-бота (старт-меню, smoke-меню, наборы, prompts) |
| `llm/DeviceLlm.kt` | движок LiteRT-LM, кольцевой лог (500), `stripThinking()` |
| `ui/navigation/NavGraph.kt` | маршруты; NavHost обёрнут в `Box(Modifier.statusBarsPadding())` |
| `ui/screens/` | Welcome, BotTree, LocalBot(правила), Buttons, Reminders, Behavior, AiSettings, BotSettings(канал+сброс), Import, Export, Packs/PackEdit, Simulate, LogScreen, AppLogScreen, FileServerScreen и др. |
| `ui/components/MenuEditor.kt` | диалог inline-кнопок (фикс: pack подставляется автоматически) |
| `ui/theme/Theme.kt` | палитра (строго!) + `lightTypography()` весь текст #F5F6F7 |

## 4. Как собирать и доставлять APK

```bash
# 1) bump версии в android/app/build.gradle.kts (СНАЧАЛА!)
#    versionCode = 22, versionName = "1.5.2" (или выше)
# 2) проверка баланса скобок после любых правок .kt (strip-strings сканер)
# 3) сборка (2–7 мин; лог в /tmp/build.log, ждём SCRIPT_EXIT=0):
cd /home/user/tg-bot-control && bash scripts/build_in_sandbox.sh > /tmp/build.log 2>&1 &
#    полл: grep "SCRIPT_EXIT=" /tmp/build.log
#    ошибки: grep -E "^e:" /tmp/build.log
# 4) проверка версии:
/opt/android-sdk/build-tools/34.0.0/aapt dump badging BotControl-debug.apk | grep ^package
# 5) очистка (обязательно, иначе раздувается workspace):
rm -rf android/app/build android/.gradle android/build android/.kotlin
# 6) present_file(BotControl-debug.apk) + сводка
```

Тулчейн (НЕ понижать): litertlm 0.17.1 → Kotlin 2.3.21 + KSP 2.3.12 →
AGP 8.12.0 → Gradle 8.13; Room 2.7.1; compileSdk 35. JDK17 сам ставится
скриптом (URL temurin 17.0.12 зашит в скрипт). После сброса песочницы
скрипт само-лечится (переустанавливает JDK/AAR/swap) — просто запустить.

## 5. Грамматика импорта (ScriptImporter) — краткая спека

Парсит: `SYSTEM_PROMPT = ( "…" "…" )`; `"temperature": X` в payload;
`LLM_MAX_TOKENS/MAX_HISTORY/ANSWER_COOLDOWN/DEFAULT_TYPING_SECONDS = N`;
`commands=[...]`; обработчики `def cmd_<имя>` (текст из send_message,
до `reply_markup`; `/start` = приветствие, `»{name}`→`{user}`);
строгие списки строк `NAME = [ "…", … ]` (`]` на отдельной строке —
обязательно, иначе список «съест» соседний код!); `CLARIFY_QUESTIONS`;
расписание — кортежи `(H, M, "текст")` внутри `get_today_schedule`:
до `if wd < 5:` = ежедневно; внутри = будни; внутри `wd == 4:` = пятница;
внутри `else:` = Пн–Чт. Дедуп по (дни, время).

Конвенции: `ПЕРЕКУР` в тексте события → кнопки 🚬/💪/😂; `»канал` в начале
текста → событие публикуется в канал; варианты текста через `" | "` →
ротация по дню года; известные наборы получают канонические id
(`pack_jokes`, `pack_smoke_done`, `pack_smoke_healthy`) — по ним работают
встроенные кнопки. Лимит текстов команд/приветствия: 2000 символов.

## 6. Зарезервированные имена (реестр — в Промт-создание-ботов.md)

- Константы: BOT_TOKEN, SYSTEM_PROMPT, LLM_MAX_TOKENS, LLM_MAX_TOP_K,
  MAX_HISTORY, HISTORY_LIMIT, ANSWER_COOLDOWN, COOLDOWN, TYPING_SECONDS,
  DEFAULT_TYPING_SECONDS.
- Служебные списки (не наборы): schedule, commands, content_types,
  messages, history, days, buttons, menu, items, payload, choices, keyboards.
- Наборы со встроенным поведением: JOKES, SMOKE_DONE_SARCASM,
  SMOKE_HEALTHY_SARCASM, CLARIFY_QUESTIONS (+алиасы SMOKE_DONE,
  SMOKE_HEALTHY, CLARIFY).
- Функции: get_today_schedule, cmd_* (только латиница), keyboard(),
  smoke_keyboard(); надписи «▶️ Запуск», «⏹ Стоп», «🚬 Покурил»,
  «💪 Остался здоровым», «😂 Рандомную шутку» — встроенные действия.

## 7. Требования пользователя (соблюдать СТРОГО)

1. **Палитра Telegram:** background #0E1621; surface/контейнеры #17212B;
   surfaceVariant #182533; primaryContainer #2B5278; primary #5288C1;
   текст #F5F6F7; вторичный текст #7F91A4; tertiary #FBBF24; outline #33475A.
   **Тёмного текста быть не должно** — `lightTypography()` задаёт F5F6F7
   всем 15 стилям. Edge-to-edge (тёмные системные панели, светлые иконки),
   NavHost в `Box(statusBarsPadding)`.
2. **Всё настройка кнопками/списками/диалогами**; редактор кода только для
   нестандартной JS-логики. Никакого экрана логина в local mode.
3. **Безопасность:** токены в EncryptedSharedPreferences (`token_$id`),
   в экспорт не попадают; никаких публичных эндпоинтов; файловый сервер —
   только вручную, локальная сеть; arbitrary exec запрещён (Rhino с
   ClassShutter, optimizationLevel -1).
4. **LLM только локально на телефоне**, без облака.
5. Мультиботы: настройки строго изолированы per-bot (botId в ключах).
6. Оракл — рабочий python-бот (`uploads/oracle code.txt`): импортированный
   бот обязан вести себя как оригинал (расписание будни+пятница, /start с
   кнопками Запуск/Стоп, перекурные кнопки, SYSTEM_PROMPT Смита,
   cooldown 10с, typing 4с, память 4).
7. Каждая итерация = **готовый APK** + понятная сводка; версия бампается.
8. Удаление ботов/настроек — с подтверждением. «Запустить/Остановить» —
   только конкретный бот, «Все боты» — отдельные кнопки.
9. Новые библиотеки — только проверенные (Maven Central HTTP 200), без
   выдуманных.

## 8. Технические ловушки (проверено на ошибках)

- **bash-инструмент:** `cwd` — только чистый путь; `cd` первой строкой
  `command`. Долгие сборки — через `start_process` + полл `/tmp/build.log`.
- **python-хередок ↔ Kotlin:** `\n` в python-строке становится переводом
  строки — замену Kotlin-строк с `\n` делать через raw-строки python
  `r'...'`; `"""` внутри хередока запрещён (использовать `'''…'''`);
  `$var` в Kotlin-шаблонах экранировать (`$min_` → `${min}`).
- Kotlin regex в коде писать только raw-строками `"""…"""`.
- Добавил `LaunchedEffect`/компонент — **проверь импорты** в файле
  (частая причина падения сборки).
- `Result.success(...)` явно оборачивать внутри fold/parse-лямбд (вывод
  типов ломается); `onSuccess` у `sendMessage` возвращает Unit — не ждать id.
- Elvis внутри `states.value + (id to x?.copy() ?: Y)` не компилируется —
  вычислять до; suspend-вызовы из лямбд — выносить.
- После правок гонять strip-strings баланс-сканер скобок по всем .kt.
- Незакрытый `Column`/скобка в диалогах AlertDialog — проверять хвост
  файла после вставки секций.
- Безвредные артефакты (не чинить): дубль `topK = it` в AiSettingsScreen;
  мёртвый regex-префикс в ScriptImporter.parse.
- Ошибки сборки обычно 0–2 цикла фиксов; после успеха — очистка build-каталогов.

## 9. История версий (доставлены)

1.0.0–1.0.5 (клиент+API+aiogram3), 1.1.0 (всё кнопками), 1.2.0 (импорт
конфигов), 1.3.0 (anti-flood, подсказки), 1.4.0 (изоляция настроек,
палитра), 1.4.1 (per-bot запуск, деревья, редактирование), 1.4.2 (Поведение
и паузы, импорт-fidelity), 1.4.3 (per-bot typing/keyboard, edge-to-edge),
1.4.4 (setMyCommands 400-fix, проблемы в карточке, журнал с копированием),
1.4.5 (экспорт настроек в код, лог в меню бота), 1.4.6 (строгая грамматика
наборов, канонические id, /help-тексты), 1.4.7 (**callback_data фикс —
пустой id ломал ВСЕ сообщения с кнопками**; сквозное логирование решений),
1.4.8 (лимит текстов 2000), 1.4.9 (файловый сервер, директории ботов,
think-фильтр, кнопки→сценарии, statusBarsPadding, промт v2), **1.5.0
(движок объявлений: визард с фото, публикация в канал, URL-кнопки,
статистика/удаление/переопределение, посты в канал с ротацией)**,
1.5.1 (фикс «кнопки не добавляются» — pack не подставлялся; сброс настроек
бота; обратная связь в «Кнопки бота»).

## 10. Что можно делать дальше (идеи, не поручено)

Из roadmap `Фичи-даромвпл.md`: настоящий медиаальбом (sendMediaGroup —
сейчас фото публикуются отдельными сообщениями), редактирование фото в
визарде (сейчас только описание), мини-БД статистики per-user, модерация
владельцем (уведомления админу, deeplink), лимиты публикаций per-user,
валидация в визардах. Плюс серверная часть (FastAPI/JWT) из исходного
 Scope — пока не развивалась.

## 11. Быстрая проверка после сборки

1. Импорт `daromvpl-channel-ready.py` → «Разобрать» (команды start/help/rules,
   уточняющие, 3 события с флагом «в канал») → «Применить».
2. Настройки бота: канал @daromvpl, «Объявления: вкл».
3. Клавиатура: 📝 Разместить объявление / 📋 Мои объявления / ❌ Отмена.
4. ▶ Запустить → /start → визард до публикации; 📋 → статистика; 🗑 удаляет
   пост из канала; в 10:20/20:40 пост в канал (тексты чередуются по дням).

## 12. Если workspace пуст (новая песочница)

Попроси пользователя загрузить `botcontrol-handoff.zip` и распакуй:
```bash
cd /home/user && python3 -c "import zipfile; zipfile.ZipFile('botcontrol-handoff.zip').extractall('/')"
```
Затем: сборка по разделу 4 (скрипт сам поставит JDK/SDK). Ораклы лежат в
`uploads/` архива.

**Первое действие в новом диалоге:** прочитать этот файл, затем
`ls /home/user/tg-bot-control`, проверить версию APK (aapt) и спросить
пользователя задачу, если её не дали в первом сообщении.
