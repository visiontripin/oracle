# ПЕРЕДАЧА КОНТЕКСТА: проект BotControl (читать ПЕРВЫМ ДЕЛОМ)

Этот файл — полная память проекта для продолжения работы в новом диалоге.
Прочитай его целиком до выполнения любой задачи. Workspace сохранён и
является источником истины; если его нет — см. раздел 12 (восстановление).

---

## 1. Что за проект

**BotControl** — Android-приложение (Kotlin + Jetpack Compose, пакет
`com.botcontrol.admin`), которое превращает телефон в хост локальных
Telegram-ботов:,long polling через Telegram Bot API, LLM **строго на
устройстве** (LiteRT-LM / MediaPipe, .litertlm модели, без облака),
мультиботы на разных токенах, всё настраивается кнопками в приложении.

Плюс формат «конфигов кодом»: настройки бота описываются python-подобным
текстом и импортируются/экспортируются приложением (это транспортный
формат, код НЕ исполняется).

Есть также серверная часть (FastAPI + aiogram3) в задумке, но активно
разрабатывался только Android-клиент (local mode).

## 2. Текущее состояние

- **Версия: v1.6.1 (versionCode 28)**
  APK: `tg-bot-control/BotControl-debug.apk` (~98 МБ, debug, пакет
  `com.botcontrol.admin.debug`). Сборка — в GitHub Actions (см. раздел 4).
- Что внутри: мультиботы, правила-сценарии (команда/фраза/кнопка →
  текст/набор/скрипт JS/ИИ), inline-кнопки с рядами и ссылками,
  клавиатура чата, напоминания с днями недели + публикация в канал
  (флаг «в канал», ротация текстов через `" | "`), режим «Объявления»,
  импорт/экспорт конфигов (**полный разбор кнопок и их обработчиков**),
  файловый сервер NanoHTTPD (:8090), журналы, проблемы бота в карточке,
  вырезание `<think>` из ответов LLM, сброс настроек бота — на главном
  экране бота, раздел «🧠 Промт для создания бота» с кнопкой «Копировать».
- **Встроенных примеров ботов в приложении больше нет** (пресет
  перкур-бота удалён: `PerkurPresets.kt`; в v1.5.3 дополнительно убраны
  кнопка «Пример» (start/help/joke) в меню Telegram и чип «перекур»
  в имитации). Остались только подсказки и промт, по которому нейросеть
  генерирует конфиг под задачу.
- **v1.5.3 — ИИ только по необходимости:** функции бота выполняются КОДОМ (правила, кнопки, наборы, скрипты, расписание); ИИ по умолчанию выключен и отвечает только в отдельных сценариях — команда `/chat вопрос` (или `/ai вопрос`) и правила с действием «ИИ». Переключатель «Включить ИИ» — на главном экране бота (с названием модели). URL-кнопки не шлют пустых ответов; импорт правил — самоисцеляющий (обновляет по паттерну, чистит дубли, в сводке — число кнопок); ненайденная кнопка пишет список известных кнопок в «Журнал событий». Промт — v4 (кратко: код = функции, ИИ = сценарии).
- **Выдача файлов пользователю — только прямыми ссылками на скачивание.**
  CI прикладывает к релизу APK и все `tg-bot-control/*-ready.py`
  (`botcontrol-apk.yml` при `[apk]`, `botcontrol-attach.yml` при изменении
  конфигов). Ссылка: `https://github.com/visiontripin/oracle/releases/download/<тег>/<файл>`.
- **v1.5.5 — починены inline-кнопки визарда объявлений** (регресс v1.5.2):
  у кнопок движка есть только `action = "lst_…"` без `id`, а `TelegramApi.toApi`
  отправлял `"cb" + label.hashCode()` → визард вставал на шаге 2 («Кнопка
  'cb-1230402332' не найдена»). Теперь `callback_data = action` для `lst_*`;
  старые сообщения распознаются через `ListingEngine.legacyCallback`. Сообщения
  визарда пишутся в журнал. **Правило: у любой InlineBtn без id callback_data
  должен быть осмысленным кодом, не хешем.**
- **v1.5.6 — переопубликация без дублей:** «🔄 Переопубликовать» удаляет
  старый пост из канала после успешной публикации нового (неудалённые id
  сохраняются для повтора); `sendTo` возвращает message_id (текстовые посты
  без фото тоже удаляются); `deleteMessage` больше не глотает все HTTP 400 —
  «not found» = ок, остальное (нет прав / старше 48 ч) = ошибка с причиной;
  «Удалить» при частичной неудаче оставляет объявление с неудалёнными id.
  Проверено пользователем на v1.5.5: весь визард (описание → контакт → фото →
  предпросмотр → публикация, «Мои объявления», переопубликация, удаление) работает.
- **v1.6.0 — «фишки»: анимация правкой одного сообщения + кубики.**
  `data/Anim.kt` (AnimSpec, шаблоны spinner/progress/dots/countdown/typewriter/
  matrix/slot/moon/clock/heart/custom, кадры, encode/decode — чистый Kotlin,
  тест `tools/kotlin-harness/src/AnimTest.kt`), `service/AnimPlayer.kt`
  (sendMessageForId → editMessageText каждые 0,5–3 с, 429 → retry_after,
  одна анимация на чат, моноширинные кадры в `<pre>`, ведущие пустые строки
  сохраняются символом ⠀). Хранение без миграции БД: спецификация — строкой
  `Anim.encode` в поле `script` (правило actionType/кнопка action = "anim");
  кубик — эмодзи в `responseText`/`btn.text` (action "dice", sendDice).
  UI: `ui/components/AnimEditor.kt` (чипы эффектов, подпись, шаг, повторы,
  итог/набор, свои кадры, живой предпросмотр) — в диалоге правила
  (чипы «🎞 Анимация», «🎲 Кубик»), в редакторе кнопки и в «Имитации».
  Импорт: `play_animation(chat, FRAMES|"preset", delay, "итог", text=,
  loops=, pack=, mono=, reply_markup=, message_id=)`, цикл `for f in FRAMES:`
  + `sleep` + `edit_message_text` прямо в обработчике, `bot.send_dice(emoji=)`;
  списки-кадры (и `ANIM_*`, `*_FRAMES`) — не наборы. Кнопки start/stop без
  обработчика → вкл/выкл напоминаний. `&lt;` и др. HTML-сущности декодируются.
  **Экспорт** теперь пишет общий `@bot.callback_query_handler` (кнопки после
  повторного импорта работают), блок ИИ — только если ИИ включён, `\n` в
  многострочных текстах, названия наборов комментарием, свои наборы дней
  (выходные и т.п.) отдельными `if wd …:` блоками, шаблоны анимаций — готовыми
  кадрами + `preset=` (файл запускается и как обычный бот).
  Тест круга «код → импорт → экспорт → импорт»: `src/RoundTrip.kt`
  (заглушка Gson — реестр объектов, хранилище и репозиторий с состоянием).
  Промт — v5 (правила 10–11: анимация, кубики).
  Готовый конфиг бота №2: `smith-bot-ready.py` (без ИИ и шуток, рабочие кнопки,
  заставка «Матрица» на /start).
  Доставка: CI 36101169804 (успех), тег `botcontrol-v1.6.0-code27`, APK
  97 869 407 байт, sha256 `805bb98a69460deb366c0d55022fcef14a403766f86dbad1b8ccc6354a1a8472`,
  сертификат прежний (`2ecc9b73…0d5e`). Нужна проверка пользователем на телефоне.
- **v1.6.1 — фото-анимация (editMessageMedia):** кадр своих кадров с первой
  строкой `photo: <https | file_id | путь>` — картинка, остальное — подпись;
  кадр без photo: меняет только подпись. `Anim.photoFrames/photoSrc/isPhotoAnim`,
  `TelegramApi.sendPhotoForId/editMessageMedia/editMessageCaption` (multipart
  для файлов с телефона), `AnimPlayer.runPhoto` + кеш file_id («botId|src»).
  Фото из галереи копируются в `files/anim/`. Если анимация по кнопке «на месте»
  не может править сообщение (текст ↔ фото) — показывается новым сообщением.
  Экспортный `play_animation` понимает photo-кадры (send_photo /
  edit_message_media / edit_message_caption). Промт v5.1.
- **versionCode следующей доставки: 29.** Всегда бампать перед сборкой.

## 3. Карта файлов (всё под `/home/user/tg-bot-control/`)

| Путь | Роль |
|---|---|
| `BotControl-debug.apk` | последний доставленный APK (v1.6.1/code 28; в git не лежит — см. раздел 4) |
| `android/` | исходники Gradle-проекта (AGP 8.12.0, Gradle 8.13, Kotlin 2.3.21, KSP 2.3.12, Room 2.7.1, compileSdk 35) |
| `android/app/build.gradle.kts` | versionCode/versionName + зависимости (в т.ч. `org.nanohttpd:nanohttpd:2.3.1`) |
| `scripts/build_in_sandbox.sh` | сборка: сам поднимает JDK17/SDK/AAR/swap; лог `/tmp/build.log`, конец по `SCRIPT_EXIT=0` |
| `bot-config-ready.py` | готовый конфиг «Агент Смит / перекур» (100 шуток, расписание будни+пятница) |
| `daromvpl-channel-ready.py` | готовый конфиг канала @daromvpl: приветствие, /help, /rules, дайджесты 10:20 и 20:40 (флаг `»канал`, ротация по дням) |
| `daromvpl-bot-ready.py`, `channel-bot-ready.py` | ранние конфиги ботов объявлений |
| `Инструкция-код-бота.md` | как писать/править конфиги + чек-лист проблем |
| `Промт-создание-ботов.md` | промт v4 для генерации конфигов (тот же текст — в приложении: Скрипты → Промт для создания бота) |
| `Инструкция-код-бота.md` | полная таблица грамматики импорта (блок кода → настройка) |
| `Фичи-даромвпл.md` | roadmap фич (P0/P1/P2) по боту объявлений |
| `ПРОМТ-НОВОЕ-ОКНО.txt` | готовый текст первого сообщения в новом диалоге |
| `botcontrol-handoff.zip` | архив исходников+доков для переноса в чистый workspace |
| `tools/kotlin-harness/` | kotlinc из PyPI: компиляция и прогон `PySource`/`ScriptImporter`/`SettingsExporter` без Android (см. раздел 4) |
| `tools/import-fixtures/` | регрессионные конфиги для импортёра + эталонные цифры разбора (оракул-перекур без токена, aiogram-оракул, демо-бот, правки пользователя) |
| `/home/user/uploads/` | ораклы: `oracle code.txt` (перкур-бот python/Termux — функциональный эталон, **в git не коммитится: там живой токен**), `oracle llm.txt`, `правки.txt` |

### Ключевые исходники (`android/app/src/main/java/com/botcontrol/admin/`)

| Файл | Роль |
|---|---|
| `service/LocalBotService.kt` | опрос Telegram per-bot, обработка сообщений/callback, планировщик напоминаний (в т.ч. `toChannel`+ротация), проблемы бота (`problems: Map<String,String>`), интеграция ListingEngine |
| `service/BotBrain.kt` | единый «мозг»: правила → скрипт/набор → ИИ (контекст) → уточняющий/запасной; кулдаун только на «незнакомых» |
| `service/ListingEngine.kt` | визард объявлений (описание→контакт→фото→предпросмотр→публикация), «Мои объявления», кнопки `lst_*` |
| `service/FileServer.kt` | NanoHTTPD :8090 — страница загрузки/скачивания файлов ботов по Wi-Fi |
| `data/telegram/TelegramApi.kt` | getUpdates/sendMessage/sendTo(канал)/sendPhoto/sendPhotoTo/getFile/downloadFile/deleteMessage/editMessageText/answerCallbackQuery/setMyCommands/deleteWebhook |
| `data/LocalBotStore.kt` | DataStore per-bot: клавиатура, промпт, ИИ-параметры, расписание, меню, канал, объявления (`listings`), `resetBot()` |
| `data/BotExtras.kt` | `InlineBtn`(+`url`), `ScheduleEvent`(+`toChannel`), `Listing`, `BotJson`, `withIds()` (детерминированные callback_data) |
| `data/PySource.kt` | мини-сканер Python-текста: строки, комментарии, логические строки, отступы, функции, выражения. Код НЕ исполняется |
| `data/ScriptImporter.kt` | грамматика импорта поверх `PySource` (см. раздел 5) |
| `data/SettingsExporter.kt` | экспорт настроек в python-конфиг |
| `data/BotFiles.kt` | директории `bot_<id>/media|scripts|docs` |
| `llm/DeviceLlm.kt` | движок LiteRT-LM, кольцевой лог (500), `stripThinking()` |
| `ui/navigation/NavGraph.kt` | маршруты; NavHost обёрнут в `Box(Modifier.statusBarsPadding())` |
| `ui/screens/` | Welcome, BotTree(дерево + сброс настроек), LocalBot(правила), Buttons(обзор кнопок), Reminders, Behavior, AiSettings, BotSettings(канал), Import, Export, **PromptScreen(промт + копировать)**, Packs/PackEdit, Simulate, LogScreen, AppLogScreen, FileServerScreen и др. |
| `ui/components/MenuEditor.kt` | диалог inline-кнопок (фикс: pack подставляется автоматически) |
| `ui/theme/Theme.kt` | палитра (строго!) + `lightTypography()` весь текст #F5F6F7 |

## 4. Как собирать и доставлять APK

**В песочнице агента собрать нельзя:** закрыты `dl.google.com`,
`maven.google.com`, Maven Central и `services.gradle.org` — ни Android SDK,
ни AGP/Compose/Room не скачать. JDK и компилятор Kotlin при желании
поднимаются из PyPI (`pip install jdk4py kotlin-jupyter-kernel` — внутри
есть `org.jetbrains.kotlin.cli.jvm.K2JVMCompiler`), но Android-зависимостей
всё равно нет. Поэтому APK собирается в GitHub Actions.

```bash
# 1) bump версии в android/app/build.gradle.kts (СНАЧАЛА!)
#    versionCode = 28, versionName = "1.6.1"
# 2) баланс скобок после правок .kt (strip-strings сканер) + вычитка импортов
# 3) коммит с [apk] в сообщении → воркфлоу соберёт, проверит aapt,
#    выложит артефакт и pre-release с APK:
git add -A && git commit -m "Что сделано [apk]"
git push origin arena/01a0d2d9-oracle
# 4) статус и результат:
gh run list -R visiontripin/oracle --limit 3
#    аннотации (в т.ч. строка aapt dump badging) — тут:
gh api repos/visiontripin/oracle/check-runs/<JOB_ID>/annotations
# 5) APK забрать из тега релиза (release-assets и blob артефактов
#    в песочнице НЕДОСТУПНЫ — gh release download / gh run download падают
#    с EOF, работает только через git):
git fetch origin refs/tags/botcontrol-v1.5.2-code22
git show botcontrol-v1.5.2-code22:tg-bot-control/BotControl-debug.apk \
  > tg-bot-control/BotControl-debug.apk
#    проверка:
python3 -c "import pyaxmlparser,sys; a=pyaxmlparser.APK(sys.argv[1]); \
print(a.packagename, a.version_code, a.version_name)" tg-bot-control/BotControl-debug.apk
# 6) очистка build-каталогов (иначе раздувается workspace):
rm -rf tg-bot-control/android/app/build tg-bot-control/android/.gradle
# 7) present_file(tg-bot-control/BotControl-debug.apk) + сводка
```

Воркфлоу: `.github/workflows/botcontrol-apk.yml` (пуш в ветку сессии,
`paths: tg-bot-control/android/**`). Собирает `gradle :app:assembleDebug`,
печатает `aapt dump badging` аннотацией, загружает артефакт
`BotControl-debug-apk` и (при `[apk]` в сообщении коммита) делает тег
`botcontrol-v<версия>-code<код>` + pre-release с APK.

**Подпись:** в `android/app/build.gradle.kts` задан постоянный debug-ключ
`android/app/debug.keystore` (пароль `android`, alias `androiddebugkey`).
Благодаря ему APK из CI ставится поверх установленного, без удаления
приложения и потери ботов. Ключ лежит в репозитории — это debug-сборка.

Локальная проверка Kotlin без Android (пригодилась для импортёра):
`jdk4py` + `kotlin-jupyter-kernel` дают `K2JVMCompiler`; файлы
`data/PySource.kt`, `data/ScriptImporter.kt`, `data/SettingsExporter.kt`,
`data/BotExtras.kt` компилируются и прогоняются на реальных конфигах
(оракул, bot-config-ready.py, demo-конфиг) без участия Android SDK.
Готовый харнесс лежит в репозитории: **`tools/kotlin-harness/`**
(`kc.sh` — компиляция, `run.sh` — запуск, `src/Dump.kt` — печать разбора,
`stubs/`, `stubs2/` — заглушки вместо Android/Gson) и конфиги-фикстуры в
`tools/import-fixtures/` с эталонными цифрами. Прогонять при каждой
правке импортёра: изменился вывод или появился warning — значит регрессия.

## 5. Грамматика импорта (PySource + ScriptImporter, v1.5.2)

Импорт больше не «регэксп по файлу»: `PySource` разбирает структуру
(строки, комментарии, логические строки, отступы, функции, выражения),
а `ScriptImporter` превращает её в настройки. Подробная таблица
блоков — в **`Инструкция-код-бота.md`** (раздел 2), здесь — суть:

- **Команды:** `@bot.message_handler(commands=["start"])` + `def cmd_start`;
  текст ответа — из `send_message` (до `reply_markup`).
- **Правила на текст:** `func=lambda m: m.text == "…"` → тип «Кнопка»;
  `"фраза" in (m.text or "").lower()` → «Содержит»; `Text(equals=…)` и
  `regexp="простой текст"` тоже понимаются.
- **Inline-меню:** функции с `InlineKeyboardMarkup` / `InlineKeyboardBuilder`;
  `markup.row(...)`, `.add(...)`, `.insert(...)`, `.adjust(2, 1)`,
  `inline_keyboard=[[…]]` — ряды сохраняются в поле `InlineBtn.row`.
  `url="…"` → кнопка-ссылка.
- **Обработчик нажатий:** `@bot.callback_query_handler` (общий или с
  фильтром `call.data == "…"`) и/или ветки `if/elif call.data == "…"`,
  `match/case`. Внутри понимаются:
  `answer_callback_query` → всплывашка (перед `return` — ответ на повторное
  нажатие); `send_message` → новое сообщение; `edit_message_text` → правка
  сообщения под кнопкой; `random.choice(НАБОР)` → действие «набор»
  (и `def get_joke(): return random.choice(JOKES)`); присваивание
  `bot_running = True/False` (или любой переменной из `remind|run|active|…`)
  → кнопка включает/выключает напоминания.
- **Клавиатура чата:** `ReplyKeyboardMarkup` + `KeyboardButton`, прикреплённая
  `reply_markup=chat_keyboard()` (приоритет у /start).
- **Наборы:** `ИМЯ = [ "…", ]` — закрывающая `]` на отдельной строке;
  комментарий на строке `ИМЯ = [` становится названием набора.
- **Расписание:** кортежи в `get_today_schedule(now)`; 4-й элемент —
  меню под напоминанием; зоны дней — любые `if/elif/else` по
  `wd` / `weekday()` / `isoweekday()`, включая `in (5, 6)` и диапазоны.
  `»канал` в начале текста → в канал; варианты `" | "` → ротация по дням.
- **Анимации и кубики (v1.6.0):** `play_animation(...)` (любая функция без
  декоратора с `for` + `edit_message_text` считается помощником анимации),
  цикл правок прямо в обработчике, `bot.send_dice(chat, emoji="🎯")`.
- **Прочее:** `bot.set_my_commands([...])` → меню Telegram;
  `CHANNEL_ID`, `LISTINGS_MODE` → настройки канала и объявлений.
- **Замечания:** всё, что понять не удалось (кнопка без обработчика, меню
  без `reply_markup`, набор по ссылке без списка, слишком сложный фильтр),
  попадает в `Parsed.warnings` и показывается на экране импорта.

## 6. Зарезервированные имена (реестр — в Промт-создание-ботов.md)

- Константы: BOT_TOKEN, SYSTEM_PROMPT, LLM_MAX_TOKENS, LLM_MAX_TOP_K,
  MAX_HISTORY, HISTORY_LIMIT, ANSWER_COOLDOWN, COOLDOWN, TYPING_SECONDS,
  DEFAULT_TYPING_SECONDS.
- Служебные списки (не наборы): schedule, commands, content_types,
  messages, history, days, buttons, menu, items, payload, choices, keyboards.
- Наборы со встроенным поведением: JOKES, SMOKE_DONE_SARCASM,
  SMOKE_HEALTHY_SARCASM, CLARIFY_QUESTIONS (+алиасы SMOKE_DONE,
  SMOKE_HEALTHY, CLARIFY).
- Функции: get_today_schedule, cmd_* (только латиница), keyboard(),
  smoke_keyboard(); надписи «▶️ Запуск», «⏹ Стоп», «🚬 Покурил»,
  «💪 Остался здоровым», «😂 Рандомную шутку» — встроенные действия.

## 7. Требования пользователя (соблюдать СТРОГО)

1. **Палитра Telegram:** background #0E1621; surface/контейнеры #17212B;
   surfaceVariant #182533; primaryContainer #2B5278; primary #5288C1;
   текст #F5F6F7; вторичный текст #7F91A4; tertiary #FBBF24; outline #33475A.
   **Тёмного текста быть не должно** — `lightTypography()` задаёт F5F6F7
   всем 15 стилям. Edge-to-edge (тёмные системные панели, светлые иконки),
   NavHost в `Box(statusBarsPadding)`.
2. **Всё настройка кнопками/списками/диалогами**; редактор кода только для
   нестандартной JS-логики. Никакого экрана логина в local mode.
3. **Безопасность:** токены в EncryptedSharedPreferences (`token_$id`),
   в экспорт не попадают; никаких публичных эндпоинтов; файловый сервер —
   только вручную, локальная сеть; arbitrary exec запрещён (Rhino с
   ClassShutter, optimizationLevel -1).
4. **LLM только локально на телефоне**, без облака.
5. Мультиботы: настройки строго изолированы per-bot (botId в ключах).
6. Оракл — рабочий python-бот (`uploads/oracle code.txt`): импортированный
   бот обязан вести себя как оригинал (расписание будни+пятница, /start с
   кнопками Запуск/Стоп, перекурные кнопки, SYSTEM_PROMPT Смита,
   cooldown 10с, typing 4с, память 4).
7. Каждая итерация = **готовый APK** + понятная сводка; версия бампается.
8. Удаление ботов/настроек — с подтверждением. «Запустить/Остановить» —
   только конкретный бот, «Все боты» — отдельные кнопки.
9. Новые библиотеки — только проверенные (Maven Central HTTP 200), без
   выдуманных.

## 8. Технические ловушки (проверено на ошибках)

- **bash-инструмент:** `cwd` — только чистый путь; `cd` первой строкой
  `command`. Долгие сборки — через `start_process` + полл `/tmp/build.log`.
- **python-хередок ↔ Kotlin:** `\n` в python-строке становится переводом
  строки — замену Kotlin-строк с `\n` делать через raw-строки python
  `r'...'`; `"""` внутри хередока запрещён (использовать `'''…'''`);
  `$var` в Kotlin-шаблонах экранировать (`$min_` → `${min}`).
- Kotlin regex в коде писать только raw-строками `"""…"""`.
- Добавил `LaunchedEffect`/компонент — **проверь импорты** в файле
  (частая причина падения сборки).
- `Result.success(...)` явно оборачивать внутри fold/parse-лямбд (вывод
  типов ломается); `onSuccess` у `sendMessage` возвращает Unit — не ждать id.
- Elvis внутри `states.value + (id to x?.copy() ?: Y)` не компилируется —
  вычислять до; suspend-вызовы из лямбд — выносить.
- После правок гонять strip-strings баланс-сканер скобок по всем .kt.
- Незакрытый `Column`/скобка в диалогах AlertDialog — проверять хвост
  файла после вставки секций.
- Безвредные артефакты (не чинить): дубль `topK = it` в AiSettingsScreen;
  мёртвый regex-префикс в ScriptImporter.parse.
- Ошибки сборки обычно 0–2 цикла фиксов; после успеха — очистка build-каталогов.

## 9. История версий (доставлены)

1.0.0–1.0.5 (клиент+API+aiogram3), 1.1.0 (всё кнопками), 1.2.0 (импорт
конфигов), 1.3.0 (anti-flood, подсказки), 1.4.0 (изоляция настроек,
палитра), 1.4.1 (per-bot запуск, деревья, редактирование), 1.4.2 (Поведение
и паузы, импорт-fidelity), 1.4.3 (per-bot typing/keyboard, edge-to-edge),
1.4.4 (setMyCommands 400-fix, проблемы в карточке, журнал с копированием),
1.4.5 (экспорт настроек в код, лог в меню бота), 1.4.6 (строгая грамматика
наборов, канонические id, /help-тексты), 1.4.7 (**callback_data фикс —
пустой id ломал ВСЕ сообщения с кнопками**; сквозное логирование решений),
1.4.8 (лимит текстов 2000), 1.4.9 (файловый сервер, директории ботов,
think-фильтр, кнопки→сценарии, statusBarsPadding, промт v2), **1.5.0
(движок объявлений: визард с фото, публикация в канал, URL-кнопки,
статистика/удаление/переопределение, посты в канал с ротацией)**,
1.5.1 (фикс «кнопки не добавляются» — pack не подставлялся; сброс настроек
бота; обратная связь в «Кнопки бота»),
**1.5.2 (импорт кнопок «по-настоящему»: разбор меню И обработчиков нажатий,
ряды, URL-кнопки, клавиатура чата, правила на фразы; удалены все встроенные
примеры (пресет перкур-бота); сброс настроек — на главном экране бота;
отдельный раздел «🧠 Промт для создания бота» с «Копировать»; кнопки ищутся
только среди меню своего бота; экспорт выгружает меню и ряды).**,
**1.5.3 (ИИ только по необходимости: по умолчанию выключен, отвечает
только по сценариям — /chat и правило «ИИ»; функции — кодом; URL-кнопки без
пустых ответов; импорт правил самоисцеляющий с отчётом «кнопок: N»;
диагностика ненайденных кнопок в журнале; переключатель ИИ и сброс настроек —
на главном экране бота; убраны «Пример» и чип «перекур»; промт v4).**,
**1.5.4 (кнопка «добавить объявление» запускает визард: гибкое распознавание
намерений вместо точного совпадения надписи; кнопка без правила отвечает
подсказкой, а не случайным вопросом; подсказка про выключенный режим
«Объявления»; импорт сообщает «кнопок в скрипте нет» / «без действия: N»;
своё приветствие /start не подменяется в режиме объявлений; в готовый конфиг
daromvpl-channel-ready.py добавлены клавиатура, канал и LISTINGS_ON).**,
**1.5.5 (фикс: inline-кнопки визарда объявлений уходили с хешем надписи
вместо lst_-кода — визард вставал на шаге «контакт»; совместимость со
старыми сообщениями; журнал визарда; ложное «без действия» при импорте).**,
**1.5.6 (переопубликация убирает старый пост — в канале одно объявление;
id текстовых постов сохраняются; честные ошибки удаления с причиной).**,
**1.6.0 (фишки: анимация правкой сообщения — 10 шаблонов + свои кадры,
флипбук/ASCII/квест, слот с итогом из набора; кубики Telegram; экспорт с
обработчиком кнопок и без ИИ, если он выключен; круговой тест экспорта;
бот №2 smith-bot-ready.py).**,
**1.6.1 (фото-анимация: картинки меняются в одном сообщении — флипбук и
квест с иллюстрациями; фото из галереи или по ссылке; кеш file_id).**

## 10. Что можно делать дальше (идеи, не поручено)

Из roadmap `Фичи-даромвпл.md`: настоящий медиаальбом (sendMediaGroup —
сейчас фото публикуются отдельными сообщениями), редактирование фото в
визарде (сейчас только описание), мини-БД статистики per-user, модерация
владельцем (уведомления админу, deeplink), лимиты публикаций per-user,
валидация в визардах. Плюс серверная часть (FastAPI/JWT) из исходного
 Scope — пока не развивалась.

## 11. Быстрая проверка после сборки

1. Импорт `daromvpl-channel-ready.py` → «Разобрать» (команды start/help/rules,
   уточняющие, 3 события с флагом «в канал») → «Применить».
2. Настройки бота: канал @daromvpl, «Объявления: вкл».
3. Клавиатура: 📝 Разместить объявление / 📋 Мои объявления / ❌ Отмена.
4. ▶ Запустить → /start → визард до публикации; 📋 → статистика; 🗑 удаляет
   пост из канала; в 10:20/20:40 пост в канал (тексты чередуются по дням).

## 12. Если workspace пуст (новая песочница)

Попроси пользователя загрузить `botcontrol-handoff.zip` и распакуй:
```bash
cd /home/user && python3 -c "import zipfile; zipfile.ZipFile('botcontrol-handoff.zip').extractall('/')"
```
Затем: сборка по разделу 4 (скрипт сам поставит JDK/SDK). Ораклы лежат в
`uploads/` архива.

**Первое действие в новом диалоге:** прочитать этот файл, затем
`ls /home/user/tg-bot-control`, проверить версию APK (aapt) и спросить
пользователя задачу, если её не дали в первом сообщении.

## 13. Git — единственный источник истины

Весь проект живёт в репозитории **https://github.com/visiontripin/oracle**
(ветка сессии `arena/01a0d2d9-oracle`). Всё, что должно пережить диалог,
коммитится и пушится туда: исходники, конфиги, доки, воркфлоу,
`tools/`-харнесс и фикстуры. APK — в тегах релизов
(`botcontrol-v<версия>-code<код>`), сам файл в gitignore.

**Никогда не коммитить:** живые токены ботов (они встречаются в ораклах из
`uploads/`) и любые ключи, кроме debug-keystore.
